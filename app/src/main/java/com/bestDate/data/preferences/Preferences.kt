package com.bestDate.data.preferences

enum class Preferences {
    APP,
    ACCESS_TOKEN,
    REFRESH_TOKEN,
    FILTER_LOCATION,
    FILTER_STATUS,
    FILTER_GENDER,
    FIRST_ENTER,
    ARG_EXPIRES_AT,
    FIREBASE_TOKEN,
    LANGUAGE,
    IMAGE_SKIP_COUNT,
    QUESTIONNAIRE_SKIP_COUNT
}